enum AppButtonSizeEnum { small, medium, large }

enum LocaleFormatEnum { en }

enum UpdateOrderTradeEnum { quantity, orderValue, slider, priceOfCoin }

enum OrderTypeEnum { buy, sell }

enum SocketMessageType { ticker, orderbook, error }

enum OrderbookSide { bid, ask }

enum ShareButtonType { facebook, telegram, x, qrCode }

enum RecordType { deposit, withdraw }

enum TransactionType { reward, withdraw_records }

enum OrderPortalType { order, orderHistory, tradeHistory }

enum OrderFilterType { Type, Pair, Side }

enum WalletActionType { deposit, withdraw }
